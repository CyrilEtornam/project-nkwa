# Copy this file to .env before running npm run dev or npm run build.
# Point at the FastAPI backend. WebSocket live feed needs a host that supports
# ws:// (EC2/PM2). Behind CloudFront/Lambda the dashboard falls back to polling.
VITE_API_BASE_URL=http://54.218.53.26:8000
